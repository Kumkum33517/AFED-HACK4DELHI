[build]
builder = "nixpacks"

[deploy]
startCommand = "node_modules/.bin/tsx /app/server/node-build.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
